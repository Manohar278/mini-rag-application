# mini-rag-application
Mini RAG application that processes a document, stores embeddings in a hosted vector database, retrieves and reranks relevant text chunks, and generates answers using a language model through a Streamlit frontend.
